source 'http://rubygems.org'

gem 'rails', '3.1.0.rc5'
gem 'sqlite3'
gem 'rack', '1.3.3'
gem 'geocoder'
gem 'httparty'
gem 'twitter' # Are we using this? Should we be?
gem 'heroku'
gem 'rest-client', '~> 1.6.7' # External APIs Client
gem "geokit" # Geocoding (Is this needed with geocoder?)
gem "jquery-rails" 
gem 'paperclip' # Upload handling (REMOVE?)
gem 'pg' # PostgreSQL db adapter
gem "aws-s3", "~> 0.6.2" # REMOVE?
gem "devise" # authentication
gem "mobile-fu" # mobile detection
gem "faster_haversine", "~> 0.1.3" # Calculating distance between geocodes
gem 'stamp' # user-friendly date-time formatting
gem 'hoptoad_notifier' # exception notification
gem 'spreadsheet' # used for spreadsheet imports
gem 'taps'  # enable heroku db:push
gem 'dalli' # memcached interface

group :test do
  gem 'mocha'
  gem 'webmock'
end

